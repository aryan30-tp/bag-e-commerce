<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart - Scatch</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: #0a0a0a;
            min-height: 100vh;
            color: white;
        }

        .navbar {
            position: fixed;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            width: calc(100% - 40px);
            max-width: 1400px;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 1.2rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 900;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -0.5px;
        }

        .nav-links {
            display: flex;
            gap: 2.5rem;
            align-items: center;
        }

        .nav-links a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-links a:not(.btn-logout):hover {
            color: white;
        }

        .btn-logout {
            background: rgba(255, 68, 68, 0.15);
            color: #ff6b6b;
            padding: 0.7rem 1.8rem;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 68, 68, 0.3);
        }

        .btn-logout:hover {
            background: rgba(255, 68, 68, 0.25);
            transform: translateY(-2px);
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 140px 5% 4rem;
        }

        .page-header {
            margin-bottom: 3rem;
        }

        .page-header h1 {
            font-size: 3rem;
            font-weight: 900;
            background: linear-gradient(135deg, #ffffff 0%, #e0e0e0 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -1px;
        }

        .cart-content {
            display: grid;
            grid-template-columns: 1.5fr 500px;
            gap: 3rem;
        }

        .cart-items {
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }

        .cart-item {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 2rem;
            display: flex;
            gap: 2rem;
            transition: all 0.3s ease;
            animation: fadeInUp 0.5s ease both;
        }

        .cart-item:hover {
            border-color: rgba(255, 255, 255, 0.2);
            transform: translateX(5px);
        }

        .item-image {
            width: 140px;
            height: 140px;
            border-radius: 16px;
            object-fit: cover;
            background: #f0f0f0;
            flex-shrink: 0;
        }

        .item-details {
            flex: 1;
        }

        .item-name {
            font-size: 1.6rem;
            font-weight: 700;
            margin-bottom: 0.8rem;
        }

        .item-price {
            font-size: 1.5rem;
            font-weight: 800;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 0.5rem;
        }

        .item-discount {
            color: #43e97b;
            font-weight: 600;
            font-size: 0.95rem;
        }

        .item-actions {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
            align-items: center;
        }

        .quantity-controls {
            display: flex;
            align-items: center;
            gap: 1rem;
            background: rgba(255, 255, 255, 0.05);
            padding: 0.5rem 1rem;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .quantity-btn {
            background: rgba(102, 126, 234, 0.2);
            color: white;
            border: 1px solid rgba(102, 126, 234, 0.3);
            width: 32px;
            height: 32px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1.2rem;
            font-weight: 700;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .quantity-btn:hover {
            background: rgba(102, 126, 234, 0.4);
            transform: scale(1.1);
        }

        .quantity-display {
            font-weight: 700;
            font-size: 1.1rem;
            min-width: 30px;
            text-align: center;
        }

        .btn-remove {
            background: rgba(255, 68, 68, 0.15);
            color: #ff6b6b;
            border: 1px solid rgba(255, 68, 68, 0.3);
            padding: 0.6rem 1.5rem;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-remove:hover {
            background: rgba(255, 68, 68, 0.25);
            transform: translateY(-2px);
        }

        .cart-summary {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 2.5rem;
            height: fit-content;
            position: sticky;
            top: 120px;
        }

        .summary-title {
            font-size: 2rem;
            font-weight: 800;
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1.2rem;
            color: rgba(255, 255, 255, 0.7);
            font-size: 1.05rem;
        }

        .summary-row span:last-child {
            font-weight: 600;
            color: white;
        }

        .summary-row.total {
            font-size: 1.6rem;
            font-weight: 800;
            color: white;
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .summary-row.total span:last-child {
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .btn-checkout {
            width: 100%;
            padding: 1.3rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 14px;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            margin-top: 2rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            letter-spacing: 0.5px;
            position: relative;
            overflow: hidden;
        }

        .btn-checkout::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s ease;
        }

        .btn-checkout:hover::before {
            left: 100%;
        }

        .btn-checkout:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.5);
        }

        .empty-cart {
            grid-column: 1 / -1;
            text-align: center;
            padding: 6rem 2rem;
        }

        .empty-cart h2 {
            font-size: 2.5rem;
            color: rgba(255, 255, 255, 0.6);
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .empty-cart p {
            color: rgba(255, 255, 255, 0.4);
            font-size: 1.2rem;
            margin-bottom: 2.5rem;
        }

        .empty-cart a {
            display: inline-block;
            padding: 1.2rem 3rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 14px;
            font-weight: 700;
            transition: all 0.3s ease;
        }

        .empty-cart a:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.5);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 968px) {
            .cart-content {
                grid-template-columns: 1fr;
            }

            .cart-summary {
                position: static;
            }

            .navbar {
                padding: 1rem 1.5rem;
                width: calc(100% - 20px);
            }
        }

        @media (max-width: 640px) {
            .cart-item {
                flex-direction: column;
            }

            .item-image {
                width: 100%;
                height: 200px;
            }

            .item-actions {
                flex-direction: column;
                align-items: stretch;
            }

            .quantity-controls {
                justify-content: center;
            }

            .page-header h1 {
                font-size: 2.2rem;
            }

            .container {
                padding: 120px 5% 3rem;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="logo">Scatch</div>
        <div class="nav-links">
            <a href="/">Home</a>
            <a href="/shop">Shop</a>
            <a href="/cart">🛒 Cart</a>
            <a href="/users/logout" class="btn-logout">Logout</a>
        </div>
    </nav>

    <div class="container">
        <div class="page-header">
            <h1>Shopping Cart</h1>
        </div>

        <div class="cart-content">
            <% if(user.cart && user.cart.length > 0) { %>
                <div class="cart-items">
                    <% user.cart.forEach(item => { %>
                        <div class="cart-item">
                            <% if(item.product.image) { %>
                                <img src="data:image/jpeg;base64,<%= item.product.image.toString('base64') %>" alt="<%= item.product.name %>" class="item-image">
                            <% } else { %>
                                <div class="item-image"></div>
                            <% } %>
                            <div class="item-details">
                                <h3 class="item-name"><%= item.product.name %></h3>
                                <p class="item-price">₹<%= (item.product.price - item.product.discount) * item.quantity %></p>
                                <% if(item.product.discount > 0) { %>
                                    <p class="item-discount">Discount: ₹<%= item.product.discount %> per item</p>
                                <% } %>
                                <div class="item-actions">
                                    <div class="quantity-controls">
                                        <a href="/cart/decrease/<%= item.product._id %>" class="quantity-btn">−</a>
                                        <span class="quantity-display"><%= item.quantity %></span>
                                        <a href="/cart/increase/<%= item.product._id %>" class="quantity-btn">+</a>
                                    </div>
                                    <a href="/cart/remove/<%= item.product._id %>" class="btn-remove">Remove</a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>

                <div class="cart-summary">
                    <h2 class="summary-title">Order Summary</h2>
                    <div class="summary-row">
                        <span>Subtotal</span>
                        <span>₹<%= bill - 20 %></span>
                    </div>
                    <div class="summary-row">
                        <span>Delivery Fee</span>
                        <span>₹20</span>
                    </div>
                    <div class="summary-row total">
                        <span>Total</span>
                        <span>₹<%= bill %></span>
                    </div>
                    <button class="btn-checkout">Proceed to Checkout</button>
                </div>
            <% } else { %>
                <div class="empty-cart">
                    <h2>Your cart is empty</h2>
                    <p>Add some amazing products to get started!</p>
                    <a href="/shop">Continue Shopping</a>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
